# Gradle / Android
.gradle/
build/
local.properties
*.apk
*.ap_

# Editors
.idea/
.lsp/
.DS_Store

# Generated by tools/package-release.sh
miefquirl.apk
manifest.json

# Signing keys — the encrypted copy in secrets.yaml is the one to keep
*.jks
*.keystore

# Dedicated age key for CI — the private half belongs in the SOPS_AGE_KEY
# repository secret, never here
ci-age.key
